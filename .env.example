# Connection string do projeto Neon Postgres
DATABASE_URL=

# Chave mestra AES-256-GCM (32 bytes em base64). Gerar com: openssl rand -base64 32
# Nunca commitar o valor real — configurar nos 3 ambientes do Vercel (Development/Preview/Production).
CANVAS_TOKEN_ENCRYPTION_KEY=
